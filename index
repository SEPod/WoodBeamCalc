<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Wood Beams – Slowest Calculator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Basic styles -->
  <style>
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #f3f6fb;
      color: #0c223f;
    }
    header {
      background: #1450b8;
      color: #ffffff;
      padding: 1.5rem 2rem;
    }
    header h1 {
      margin: 0;
      font-size: 2.2rem;
      letter-spacing: 0.05em;
    }
    header p {
      margin: 0.2rem 0 0;
      opacity: 0.9;
    }

    main {
      max-width: 980px;
      margin: 1.5rem auto 3rem;
      padding: 0 1rem 3rem;
    }

    .step-nav {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 1.5rem;
      overflow-x: auto;
    }
    .step-chip {
      padding: 0.45rem 0.9rem;
      border-radius: 999px;
      border: 1px solid #c5d4f0;
      background: #e4ecff;
      font-size: 0.85rem;
      white-space: nowrap;
      cursor: pointer;
      user-select: none;
    }
    .step-chip.active {
      background: #1450b8;
      border-color: #1450b8;
      color: #ffffff;
      font-weight: 600;
    }
    .step-chip.completed {
      background: #18a06a;
      border-color: #18a06a;
      color: #ffffff;
    }

    .step {
      display: none;
    }
    .step.active {
      display: block;
    }

    .card {
      background: #ffffff;
      padding: 1.5rem 1.75rem;
      border-radius: 12px;
      box-shadow: 0 10px 25px rgba(15, 35, 70, 0.04);
      margin-bottom: 1.25rem;
    }

    h2 {
      margin-top: 0;
      margin-bottom: 1rem;
      font-size: 1.3rem;
    }
    h3 {
      margin: 1rem 0 0.6rem;
      font-size: 1.05rem;
    }
    p {
      margin: 0.4rem 0;
      line-height: 1.5;
    }

    .grid-2 {
      display: grid;
      grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
      gap: 1rem;
    }

    label {
      font-size: 0.9rem;
      display: block;
      margin-bottom: 0.25rem;
    }
    input[type="number"],
    input[type="text"],
    select {
      width: 100%;
      padding: 0.4rem 0.55rem;
      border-radius: 6px;
      border: 1px solid #c7d2e5;
      font-size: 0.9rem;
    }

    .radio-row {
      display: flex;
      align-items: center;
      gap: 0.4rem;
      margin: 0.2rem 0;
      font-size: 0.9rem;
    }

    .pill-toggle {
      display: inline-flex;
      border-radius: 999px;
      border: 1px solid #c5d4f0;
      overflow: hidden;
      margin-top: 0.25rem;
    }
    .pill-toggle button {
      border: none;
      padding: 0.35rem 0.85rem;
      background: transparent;
      font-size: 0.85rem;
      cursor: pointer;
    }
    .pill-toggle button.active {
      background: #1450b8;
      color: #ffffff;
    }

    .btn-primary {
      display: inline-block;
      padding: 0.65rem 1.5rem;
      border-radius: 999px;
      border: none;
      background: #f4c542;
      color: #111827;
      font-weight: 600;
      font-size: 0.95rem;
      cursor: pointer;
      margin-top: 1rem;
      box-shadow: 0 6px 0 #d9a926;
    }
    .btn-primary:active {
      transform: translateY(1px);
      box-shadow: 0 4px 0 #d9a926;
    }
    .btn-secondary {
      border-radius: 999px;
      border: 1px solid #c5d4f0;
      background: #eef3ff;
      padding: 0.5rem 1.2rem;
      font-size: 0.85rem;
      margin-left: 0.75rem;
      cursor: pointer;
      margin-top: 1rem;
    }

    .feedback {
      margin-top: 0.75rem;
      font-size: 0.9rem;
      padding: 0.6rem 0.8rem;
      border-radius: 8px;
      background: #f5f7ff;
      border-left: 3px solid #c5d4f0;
    }
    .feedback.ok {
      background: #ebf9f3;
      border-left-color: #18a06a;
    }
    .feedback.error {
      background: #fff5f5;
      border-left-color: #e02424;
    }

    .small-note {
      font-size: 0.8rem;
      color: #6b7280;
      margin-top: 0.3rem;
    }

    .report-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.9rem;
      margin-top: 0.75rem;
    }
    .report-table th,
    .report-table td {
      border: 1px solid #d1d5db;
      padding: 0.35rem 0.5rem;
      text-align: left;
    }
    .badge-pass, .badge-fail {
      display: inline-block;
      padding: 0.2rem 0.6rem;
      border-radius: 999px;
      font-size: 0.75rem;
      font-weight: 600;
      margin-left: 0.3rem;
    }
    .badge-pass {
      background: #ebf9f3;
      color: #166534;
    }
    .badge-fail {
      background: #fef2f2;
      color: #b91c1c;
    }

    @media (max-width: 720px) {
      .grid-2 {
        grid-template-columns: minmax(0, 1fr);
      }
      header {
        padding: 1.25rem 1.25rem;
      }
      main {
        padding: 0 0.75rem 2.5rem;
      }
    }
  </style>

  <!-- MathJax for LaTeX rendering -->
  <script src="https://polyfill.io/v3/polyfill.min.js?features=es6"></script>
  <script id="MathJax-script" async
    src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
</head>

<body>
  <header>
    <h1>WOOD BEAMS</h1>
    <p>The world’s slowest calculator and fastest learning tool.</p>
  </header>

  <main>
    <!-- Step nav -->
    <div class="step-nav">
      <div class="step-chip active" data-step="0">Step 0 · Choose Beam</div>
      <div class="step-chip" data-step="1">Step 1 · Moment</div>
      <div class="step-chip" data-step="2">Step 2 · Shear</div>
      <div class="step-chip" data-step="3">Step 3 · Deflection</div>
      <div class="step-chip" data-step="4">Report</div>
    </div>

    <!-- STEP 0: CHOOSE BEAM -->
    <section class="step active" id="step-0">
      <div class="card">
        <h2>Step 0 of 4 – Choose Your Beam</h2>
        <p>Set up a simple-span wood beam with uniform load. We’ll walk through the math step by step.</p>

        <div class="grid-2">
          <div>
            <h3>Geometry &amp; Material</h3>
            <label>Span length \(L\) (ft)</label>
            <input type="number" id="L-ft" value="16" step="0.1" />

            <label style="margin-top:0.6rem;">Width \(b\) (in)</label>
            <input type="number" id="b-in" value="3.5" step="0.01" />

            <label style="margin-top:0.6rem;">Depth \(d\) (in)</label>
            <input type="number" id="d-in" value="11.25" step="0.01" />

            <label style="margin-top:0.6rem;">Species / grade</label>
            <select id="species">
              <option>No. 2 DF-L (example)</option>
              <option>No. 2 SPF</option>
              <option>No. 1 Southern Pine</option>
            </select>

            <label style="margin-top:0.6rem;">Design method</label>
            <div class="pill-toggle" id="design-method-toggle">
              <button type="button" data-value="ASD" class="active">ASD</button>
              <button type="button" data-value="LRFD">LRFD</button>
            </div>

            <div class="small-note">
              Right now we’ll behave like ASD (service-level stresses), even if you flip to LRFD.
            </div>
          </div>

          <div>
            <h3>Loads &amp; Derived Section Properties</h3>
            <label>Dead load \(w_D\) (lb/ft)</label>
            <input type="number" id="wD" value="200" step="5" />

            <label style="margin-top:0.6rem;">Live load \(w_L\) (lb/ft)</label>
            <input type="number" id="wL" value="400" step="5" />

            <p class="small-note">
              For ASD we’ll start with \( w = 1.0 w_D + 1.0 w_L \).
            </p>

            <div id="derived-properties" class="small-note" style="margin-top:0.75rem;">
              <!-- Filled by JS -->
            </div>
          </div>
        </div>

        <button class="btn-primary" id="btn-to-moment">Start with Moment</button>
      </div>
    </section>

    <!-- STEP 1: MOMENT -->
    <section class="step" id="step-1">
      <div class="card">
        <h2>Step 1 of 4 – Moment &amp; Bending</h2>

        <h3>1. Maximum moment \(M_{\max}\)</h3>
        <p>For a simply supported beam with uniform load \(w\) (lb/ft) over span \(L\) (ft), pick the correct formula for the <strong>maximum bending moment</strong> \(M_{\max}\).</p>

        <div class="radio-row">
          <input type="radio" name="momentFormula" id="momA" value="correct" />
          <label for="momA">\( M_{\max} = \dfrac{w L^{2}}{8} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="momentFormula" id="momB" value="wrong1" />
          <label for="momB">\( M_{\max} = \dfrac{w L}{8} \)</label>
        </div        >
        <div class="radio-row">
          <input type="radio" name="momentFormula" id="momC" value="wrong2" />
          <label for="momC">\( M_{\max} = \dfrac{w L^{2}}{2} \)</label>
        </div>
        <p class="small-note">Units check: \(w\,[\text{lb/ft}] \times L^{2}[\text{ft}^2] \Rightarrow \text{lb·ft}\).</p>

        <hr style="margin:1.25rem 0; border:none; border-top:1px solid #e5e7eb;">

        <h3>2. Section modulus \(S\)</h3>
        <p>For a rectangular section of width \(b\) and depth \(d\), choose the correct section modulus.</p>
        <div class="radio-row">
          <input type="radio" name="SFormula" id="sA" value="correct" />
          <label for="sA">\( S = \dfrac{b d^{2}}{6} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="SFormula" id="sB" value="wrong1" />
          <label for="sB">\( S = \dfrac{b d}{6} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="SFormula" id="sC" value="wrong2" />
          <label for="sC">\( S = \dfrac{b d^{3}}{12} \)</label>
        </div>

        <h3>3. Bending stress \(f_b\)</h3>
        <p>Pick the correct relationship between \(f_b\), \(M_{\max}\) and \(S\).</p>
        <div class="radio-row">
          <input type="radio" name="fbFormula" id="fbA" value="correct" />
          <label for="fbA">\( f_b = \dfrac{M_{\max}}{S} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="fbFormula" id="fbB" value="wrong1" />
          <label for="fbB">\( f_b = \dfrac{S}{M_{\max}} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="fbFormula" id="fbC" value="wrong2" />
          <label for="fbC">\( f_b = M_{\max} \, S \)</label>
        </div>

        <h3>4. Adjusted allowable stress \(F'_b\)</h3>
        <p>For ASD bending per NDS, choose the correct expression for adjusted allowable bending stress.</p>
        <div class="radio-row">
          <input type="radio" name="FbAdjFormula" id="FbA" value="correct" />
          <label for="FbA">\( F'_b = F_b \, C_D \, C_M \, C_t \, C_F \, C_i \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="FbAdjFormula" id="FbB" value="wrong1" />
          <label for="FbB">\( F'_b = \dfrac{F_b}{C_D C_M C_t} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="FbAdjFormula" id="FbC" value="wrong2" />
          <label for="FbC">\( F'_b = F_b \, C_D \)</label>
        </div>

        <h3>5. Pass / fail inequality</h3>
        <p>Which inequality represents a <strong>passing</strong> bending check in ASD?</p>
        <div class="radio-row">
          <input type="radio" name="ineq" id="ineqA" value="correct" />
          <label for="ineqA">\( f_b \le F'_b \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="ineq" id="ineqB" value="wrong1" />
          <label for="ineqB">\( f_b \ge F'_b \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="ineq" id="ineqC" value="wrong2" />
          <label for="ineqC">Beam passes only if \( f_b = F'_b \) exactly.</label>
        </div>

        <h3 style="margin-top:1.25rem;">Optional: design values for the report</h3>
        <div class="grid-2">
          <div>
            <label>Base bending stress \(F_b\) (psi)</label>
            <input type="number" id="Fb-base" value="1000" step="10" />
          </div>
          <div>
            <label>Duration factor \(C_D\)</label>
            <input type="number" id="Cd-b" value="1.0" step="0.05" />
          </div>
          <div>
            <label>Moisture factor \(C_M\)</label>
            <input type="number" id="Cm-b" value="1.0" step="0.05" />
          </div>
          <div>
            <label>Temperature factor \(C_t\)</label>
            <input type="number" id="Ct-b" value="1.0" step="0.05" />
          </div>
          <div>
            <label>Size factor \(C_F\)</label>
            <input type="number" id="Cf-b" value="1.0" step="0.05" />
          </div>
          <div>
            <label>Incising factor \(C_i\)</label>
            <input type="number" id="Ci-b" value="1.0" step="0.05" />
          </div>
        </div>

        <button class="btn-primary" id="btn-check-moment">Check Moment</button>
        <button class="btn-secondary" id="btn-to-shear" style="display:none;">Next: Check Shear</button>

        <div id="moment-feedback" class="feedback" style="display:none;"></div>
      </div>
    </section>

    <!-- STEP 2: SHEAR -->
    <section class="step" id="step-2">
      <div class="card">
        <h2>Step 2 of 4 – Shear</h2>

        <h3>1. Maximum shear \(V_{\max}\)</h3>
        <p>For the same simple span and uniform load, what is the <strong>maximum end reaction / shear</strong> \(V_{\max}\)?</p>

        <div class="radio-row">
          <input type="radio" name="VFormula" id="V1" value="correct" />
          <label for="V1">\( V_{\max} = \dfrac{w L}{2} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="VFormula" id="V2" value="wrong1" />
          <label for="V2">\( V_{\max} = w L^{2} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="VFormula" id="V3" value="wrong2" />
          <label for="V3">\( V_{\max} = \dfrac{w}{2 L} \)</label>
        </div>

        <h3>2. Shear stress \(f_v\) in a rectangular section</h3>
        <p>For a rectangular section of area \(A = b d\), pick the correct average shear stress expression used in NDS.</p>

        <div class="radio-row">
          <input type="radio" name="fvFormula" id="fv1" value="correct" />
          <label for="fv1">\( f_v = \dfrac{3}{2}\dfrac{V_{\max}}{A} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="fvFormula" id="fv2" value="wrong1" />
          <label for="fv2">\( f_v = \dfrac{V_{\max}}{S} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="fvFormula" id="fv3" value="wrong2" />
          <label for="fv3">\( f_v = \dfrac{M_{\max}}{b d} \)</label>
        </div>

        <h3>3. Adjusted allowable shear \(F'_v\)</h3>
        <p>Choose the correct expression for adjusted allowable shear stress in ASD.</p>
        <div class="radio-row">
          <input type="radio" name="FvAdjFormula" id="Fv1" value="correct" />
          <label for="Fv1">\( F'_v = F_v \, C_D \, C_M \, C_t \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="FvAdjFormula" id="Fv2" value="wrong1" />
          <label for="Fv2">\( F'_v = \dfrac{F_v}{C_D C_M} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="FvAdjFormula" id="Fv3" value="wrong2" />
          <label for="Fv3">\( F'_v = F_v C_D C_M C_t C_F \)</label>
        </div>

        <h3>4. Pass / fail inequality</h3>
        <p>Which inequality represents a <strong>passing</strong> shear check in ASD?</p>
        <div class="radio-row">
          <input type="radio" name="ineqShear" id="ineqSv1" value="correct" />
          <label for="ineqSv1">\( f_v \le F'_v \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="ineqShear" id="ineqSv2" value="wrong1" />
          <label for="ineqSv2">\( f_v \ge F'_v \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="ineqShear" id="ineqSv3" value="wrong2" />
          <label for="ineqSv3">Check passes only if \( f_v = F'_v \).</label>
        </div>

        <h3 style="margin-top:1.25rem;">Optional: design values for the report</h3>
        <div class="grid-2">
          <div>
            <label>Base shear stress \(F_v\) (psi)</label>
            <input type="number" id="Fv-base" value="135" step="5" />
          </div>
          <div>
            <label>Duration factor \(C_D\)</label>
            <input type="number" id="Cd-v" value="1.0" step="0.05" />
          </div>
          <div>
            <label>Moisture factor \(C_M\)</label>
            <input type="number" id="Cm-v" value="1.0" step="0.05" />
          </div>
          <div>
            <label>Temperature factor \(C_t\)</label>
            <input type="number" id="Ct-v" value="1.0" step="0.05" />
          </div>
        </div>

        <button class="btn-primary" id="btn-check-shear">Check Shear</button>
        <button class="btn-secondary" id="btn-to-deflection" style="display:none;">Next: Check Deflection</button>

        <div id="shear-feedback" class="feedback" style="display:none;"></div>
      </div>
    </section>

    <!-- STEP 3: DEFLECTION -->
    <section class="step" id="step-3">
      <div class="card">
        <h2>Step 3 of 4 – Deflection</h2>

        <h3>1. Maximum deflection \( \Delta_{\max} \)</h3>
        <p>For the same simple span and uniform load, choose the correct formula for maximum deflection at midspan.</p>

        <div class="radio-row">
          <input type="radio" name="DeltaFormula" id="D1" value="correct" />
          <label for="D1">\( \Delta_{\max} = \dfrac{5 w L^{4}}{384 E I} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="DeltaFormula" id="D2" value="wrong1" />
          <label for="D2">\( \Delta_{\max} = \dfrac{w L^{3}}{48 E I} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="DeltaFormula" id="D3" value="wrong2" />
          <label for="D3">\( \Delta_{\max} = \dfrac{w L^{4}}{8 E I} \)</label>
        </div>

        <h3>2. Deflection limit</h3>
        <p>For a floor supporting typical finishes, pick a common live-load deflection limit.</p>

        <div class="radio-row">
          <input type="radio" name="DeflLimitFormula" id="DL1" value="correct" />
          <label for="DL1">\( \Delta_{\text{allow}} = \dfrac{L}{360} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="DeflLimitFormula" id="DL2" value="wrong1" />
          <label for="DL2">\( \Delta_{\text{allow}} = \dfrac{L}{120} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="DeflLimitFormula" id="DL3" value="wrong2" />
          <label for="DL3">\( \Delta_{\text{allow}} = \dfrac{L}{60} \)</label>
        </div>

        <h3>3. Pass / fail inequality</h3>
        <p>Which inequality represents a <strong>passing</strong> deflection check?</p>
        <div class="radio-row">
          <input type="radio" name="DeflIneq" id="DI1" value="correct" />
          <label for="DI1">\( \Delta_{\max} \le \Delta_{\text{allow}} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="DeflIneq" id="DI2" value="wrong1" />
          <label for="DI2">\( \Delta_{\max} \ge \Delta_{\text{allow}} \)</label>
        </div>
        <div class="radio-row">
          <input type="radio" name="DeflIneq" id="DI3" value="wrong2" />
          <label for="DI3">Check passes only if \( \Delta_{\max} = \Delta_{\text{allow}} \).</label>
        </div>

        <h3 style="margin-top:1.25rem;">Optional: stiffness values for the report</h3>
        <div class="grid-2">
          <div>
            <label>Elastic modulus \(E\) (psi)</label>
            <input type="number" id="E-psi" value="1600000" step="50000" />
          </div>
          <div>
            <label>Deflection limit ratio (e.g. 360 for \(L/360\))</label>
            <input type="number" id="defl-ratio" value="360" step="10" />
          </div>
        </div>

        <button class="btn-primary" id="btn-check-deflection">Check Deflection</button>
        <button class="btn-secondary" id="btn-to-report" style="display:none;">Create Report</button>

        <div id="deflection-feedback" class="feedback" style="display:none;"></div>
      </div>
    </section>

    <!-- STEP 4: REPORT -->
    <section class="step" id="step-4">
      <div class="card">
        <h2>Beam Report</h2>
        <p>Summary of your beam, loads, and conceptual + numeric checks.</p>

        <h3>Inputs</h3>
        <table class="report-table">
          <tbody>
            <tr><th>Span \(L\)</th><td id="r-L"></td></tr>
            <tr><th>Section</th><td id="r-section"></td></tr>
            <tr><th>Loads</th><td id="r-loads"></td></tr>
          </tbody>
        </table>

        <h3 style="margin-top:1.25rem;">Numeric Results (prototype)</h3>
        <table class="report-table">
          <thead>
            <tr><th>Quantity</th><th>Value</th></tr>
          </thead>
          <tbody>
            <tr><td>Maximum moment \(M_{\max}\)</td><td id="r-M"></td></tr>
            <tr><td>Maximum shear \(V_{\max}\)</td><td id="r-V"></td></tr>
            <tr><td>Maximum deflection \(\Delta_{\max}\)</td><td id="r-Delta"></td></tr>
            <tr><td>Section modulus \(S\)</td><td id="r-S"></td></tr>
            <tr><td>Moment of inertia \(I\)</td><td id="r-I"></td></tr>
            <tr><td>Bending stress \(f_b\)</td><td id="r-fb"></td></tr>
            <tr><td>Allowable bending \(F'_b\)</td><td id="r-Fb"></td></tr>
            <tr><td>Shear stress \(f_v\)</td><td id="r-fv"></td></tr>
            <tr><td>Allowable shear \(F'_v\)</td><td id="r-Fv"></td></tr>
            <tr><td>Deflection limit \(\Delta_{\text{allow}}\)</td><td id="r-Delta-allow"></td></tr>
          </tbody>
        </table>

        <h3 style="margin-top:1.25rem;">Conceptual Checks</h3>
        <table class="report-table">
          <thead>
            <tr><th>Topic</th><th>Status</th></tr>
          </thead>
          <tbody>
            <tr><td>Moment &amp; bending logic</td><td id="r-moment-status"></td></tr>
            <tr><td>Shear logic</td><td id="r-shear-status"></td></tr>
            <tr><td>Deflection logic</td><td id="r-defl-status"></td></tr>
          </tbody>
        </table>

        <p class="small-note" style="margin-top:1rem;">
          If this felt like a lot of steps for one beam, congratulations: you’re thinking like a structural engineer.
        </p>
      </div>
    </section>
  </main>

  <!-- App logic -->
  <script>
    const state = {
      method: "ASD",
      conceptual: {
        moment: false,
        shear: false,
        deflection: false,
      },
    };

    // ----- helpers -----
    function selectStep(stepIndex) {
      document.querySelectorAll(".step").forEach(sec => {
        sec.classList.remove("active");
      });
      document.querySelector(`#step-${stepIndex}`).classList.add("active");

      document.querySelectorAll(".step-chip").forEach(chip => {
        const s = Number(chip.dataset.step);
        chip.classList.remove("active");
        if (s === stepIndex) chip.classList.add("active");
      });

      if (window.MathJax && MathJax.typesetPromise) {
        MathJax.typesetPromise();
      }
    }

    function updateDerived() {
      const Lft = Number(document.getElementById("L-ft").value || 0);
      const Lin = Lft * 12;
      const b = Number(document.getElementById("b-in").value || 0);
      const d = Number(document.getElementById("d-in").value || 0);

      const wD = Number(document.getElementById("wD").value || 0);
      const wL = Number(document.getElementById("wL").value || 0);
      const w = wD + wL;

      const A = b * d;
      const S = (b * d * d) / 6;
      const I = (b * d * d * d) / 12;

      const div = document.getElementById("derived-properties");
      div.innerHTML = `
        <strong>Derived values (geom only):</strong><br>
        L = ${Lft.toFixed(2)} ft = ${Lin.toFixed(1)} in<br>
        A = b d = ${A.toFixed(1)} in²<br>
        S = b d² / 6 ≈ ${S.toFixed(1)} in³<br>
        I = b d³ / 12 ≈ ${I.toFixed(1)} in⁴<br>
        w = w_D + w_L = ${w.toFixed(1)} lb/ft
      `;
    }

    function showFeedback(id, message, ok) {
      const box = document.getElementById(id);
      box.style.display = "block";
      box.textContent = message;
      box.classList.remove("ok", "error");
      if (ok === true) box.classList.add("ok");
      else if (ok === false) box.classList.add("error");
    }

    // ----- report calculations -----
    function fillReport() {
      const Lft = Number(document.getElementById("L-ft").value || 0);
      const Lin = Lft * 12;
      const b = Number(document.getElementById("b-in").value || 0);
      const d = Number(document.getElementById("d-in").value || 0);
      const wD = Number(document.getElementById("wD").value || 0);
      const wL = Number(document.getElementById("wL").value || 0);
      const w_ft = wD + wL;
      const w_in = w_ft / 12;
      const species = document.getElementById("species").value;

      const A = b * d;
      const S = (b * d * d) / 6;
      const I = (b * d * d * d) / 12;

      // bending values
      const M_lbft = w_ft * Lft * Lft / 8;
      const M_kipft = M_lbft / 1000;
      const M_lbin = M_lbft * 12;
      const fb = S > 0 ? M_lbin / S : 0;

      const Fb_base = Number(document.getElementById("Fb-base").value || 0);
      const Cd_b = Number(document.getElementById("Cd-b").value || 1);
      const Cm_b = Number(document.getElementById("Cm-b").value || 1);
      const Ct_b = Number(document.getElementById("Ct-b").value || 1);
      const Cf_b = Number(document.getElementById("Cf-b").value || 1);
      const Ci_b = Number(document.getElementById("Ci-b").value || 1);
      const Fb_adj = Fb_base * Cd_b * Cm_b * Ct_b * Cf_b * Ci_b;

      // shear values
      const V_lb = w_ft * Lft / 2;
      const fv = A > 0 ? 1.5 * V_lb / A : 0;

      const Fv_base = Number(document.getElementById("Fv-base").value || 0);
      const Cd_v = Number(document.getElementById("Cd-v").value || 1);
      const Cm_v = Number(document.getElementById("Cm-v").value || 1);
      const Ct_v = Number(document.getElementById("Ct-v").value || 1);
      const Fv_adj = Fv_base * Cd_v * Cm_v * Ct_v;

      // deflection values
      const E = Number(document.getElementById("E-psi").value || 0);
      const ratio = Number(document.getElementById("defl-ratio").value || 360);
      const Delta_max = (E > 0 && I > 0)
        ? (5 * w_in * Math.pow(Lin, 4) / (384 * E * I))
        : 0;
      const Delta_allow = ratio > 0 ? Lin / ratio : 0;

      // fill input summary
      document.getElementById("r-L").textContent =
        `${Lft.toFixed(2)} ft (${Lin.toFixed(1)} in)`;
      document.getElementById("r-section").textContent =
        `${b.toFixed(2)} in × ${d.toFixed(2)} in, ${species}`;
      document.getElementById("r-loads").textContent =
        `w_D = ${wD.toFixed(1)} lb/ft, w_L = ${wL.toFixed(1)} lb/ft, w = ${w_ft.toFixed(1)} lb/ft`;

      // numeric table
      document.getElementById("r-M").textContent =
        `${M_kipft.toFixed(2)} kip·ft`;
      document.getElementById("r-V").textContent =
        `${V_lb.toFixed(0)} lb`;
      document.getElementById("r-Delta").textContent =
        `${Delta_max.toFixed(3)} in`;
      document.getElementById("r-S").textContent =
        `${S.toFixed(1)} in³`;
      document.getElementById("r-I").textContent =
        `${I.toFixed(1)} in⁴`;
      document.getElementById("r-fb").textContent =
        `${fb.toFixed(0)} psi`;
      document.getElementById("r-Fb").textContent =
        `${Fb_adj.toFixed(0)} psi`;
      document.getElementById("r-fv").textContent =
        `${fv.toFixed(1)} psi`;
      document.getElementById("r-Fv").textContent =
        `${Fv_adj.toFixed(1)} psi`;
      document.getElementById("r-Delta-allow").textContent =
        `${Delta_allow.toFixed(3)} in (L/${ratio.toFixed(0)})`;

      // conceptual status
      const momentCell = document.getElementById("r-moment-status");
      const shearCell = document.getElementById("r-shear-status");
      const deflCell = document.getElementById("r-defl-status");

      momentCell.innerHTML = state.conceptual.moment
        ? 'Moment logic passed<span class="badge-pass">OK</span>'
        : 'Not yet passed<span class="badge-fail">Check math</span>';

      shearCell.innerHTML = state.conceptual.shear
        ? 'Shear logic passed<span class="badge-pass">OK</span>'
        : 'Not yet passed<span class="badge-fail">Check math</span>';

      deflCell.innerHTML = state.conceptual.deflection
        ? 'Deflection logic passed<span class="badge-pass">OK</span>'
        : 'Not yet passed<span class="badge-fail">Check math</span>';
    }

    // ----- event wiring -----
    document.addEventListener("DOMContentLoaded", () => {
      updateDerived();

      document.querySelectorAll("#L-ft,#b-in,#d-in,#wD,#wL").forEach(el => {
        el.addEventListener("input", updateDerived);
      });

      document.querySelectorAll(".step-chip").forEach(chip => {
        chip.addEventListener("click", () => {
          const s = Number(chip.dataset.step);
          selectStep(s);
        });
      });

      // design method toggle
      document.querySelectorAll("#design-method-toggle button").forEach(btn => {
        btn.addEventListener("click", () => {
          document.querySelectorAll("#design-method-toggle button")
            .forEach(b => b.classList.remove("active"));
          btn.classList.add("active");
          state.method = btn.dataset.value;
        });
      });

      document.getElementById("btn-to-moment").addEventListener("click", () => {
        selectStep(1);
      });

      // Moment check
      document.getElementById("btn-check-moment").addEventListener("click", () => {
        const moment = document.querySelector('input[name="momentFormula"]:checked');
        const sForm = document.querySelector('input[name="SFormula"]:checked');
        const fbForm = document.querySelector('input[name="fbFormula"]:checked');
        const FbAdj = document.querySelector('input[name="FbAdjFormula"]:checked');
        const ineq = document.querySelector('input[name="ineq"]:checked');

        if (!moment || !sForm || !fbForm || !FbAdj || !ineq) {
          showFeedback("moment-feedback", "Answer all five parts before checking.", false);
          return;
        }

        const allCorrect =
          moment.value === "correct" &&
          sForm.value === "correct" &&
          fbForm.value === "correct" &&
          FbAdj.value === "correct" &&
          ineq.value === "correct";

        if (allCorrect) {
          state.conceptual.moment = true;
          showFeedback(
            "moment-feedback",
            "Nice work. Your moment, section modulus, bending stress expression, adjustment factors, and inequality all line up.",
            true
          );
          document.getElementById("btn-to-shear").style.display = "inline-block";
          document.querySelector('.step-chip[data-step="1"]').classList.add("completed");
        } else {
          state.conceptual.moment = false;
          showFeedback(
            "moment-feedback",
            "The number I’d get for bending doesn’t match your chain of equations. Check the moment formula, section modulus, stress definition, and inequality, then try again.",
            false
          );
        }
      });

      document.getElementById("btn-to-shear").addEventListener("click", () => {
        selectStep(2);
      });

      // Shear check
      document.getElementById("btn-check-shear").addEventListener("click", () => {
        const VForm = document.querySelector('input[name="VFormula"]:checked');
        const fvForm = document.querySelector('input[name="fvFormula"]:checked');
        const FvAdj = document.querySelector('input[name="FvAdjFormula"]:checked');
        const ineqS = document.querySelector('input[name="ineqShear"]:checked');

        if (!VForm || !fvForm || !FvAdj || !ineqS) {
          showFeedback("shear-feedback", "Answer all four parts before checking.", false);
          return;
        }

        const allCorrect =
          VForm.value === "correct" &&
          fvForm.value === "correct" &&
          FvAdj.value === "correct" &&
          ineqS.value === "correct";

        if (allCorrect) {
          state.conceptual.shear = true;
          showFeedback(
            "shear-feedback",
            "Shear chain looks good. Your \(V_{\max}\), shear stress expression, factors, and inequality match the usual wood design logic.",
            true
          );
          document.getElementById("btn-to-deflection").style.display = "inline-block";
          document.querySelector('.step-chip[data-step="2"]').classList.add("completed");
        } else {
          state.conceptual.shear = false;
          showFeedback(
            "shear-feedback",
            "The number I’d expect for shear doesn’t line up with those equations. The problem child is probably the \(V_{\max}\) formula or the 3/2 factor.",
            false
          );
        }
      });

      document.getElementById("btn-to-deflection").addEventListener("click", () => {
        selectStep(3);
      });

      // Deflection check
      document.getElementById("btn-check-deflection").addEventListener("click", () => {
        const DForm = document.querySelector('input[name="DeltaFormula"]:checked');
        const DLim = document.querySelector('input[name="DeflLimitFormula"]:checked');
        const DIneq = document.querySelector('input[name="DeflIneq"]:checked');

        if (!DForm || !DLim || !DIneq) {
          showFeedback("deflection-feedback", "Answer all three parts before checking.", false);
          return;
        }

        const allCorrect =
          DForm.value === "correct" &&
          DLim.value === "correct" &&
          DIneq.value === "correct";

        if (allCorrect) {
          state.conceptual.deflection = true;
          showFeedback(
            "deflection-feedback",
            "Deflection formulas and inequality look good. You’ve got the 5/384 coefficient, the \(L/360\) limit, and the comparison in the right direction.",
            true
          );
          document.getElementById("btn-to-report").style.display = "inline-block";
          document.querySelector('.step-chip[data-step="3"]').classList.add("completed");
        } else {
          state.conceptual.deflection = false;
          showFeedback(
            "deflection-feedback",
            "The deflection I’d compute either explodes or is way too forgiving. Check the 5/384 coefficient, the power on L, and which side of the inequality is larger.",
            false
          );
        }
      });

      document.getElementById("btn-to-report").addEventListener("click", () => {
        fillReport();
        document.querySelector('.step-chip[data-step="4"]').classList.add("completed");
        selectStep(4);
      });
    });
  </script>
</body>
</html> 
